# Requirements for FNA_v3
# Structured by usage category and sorted alphabetically within each section.

# Core model runtime dependencies
# NOTE: the model is fully headless — Excel I/O goes through openpyxl, so no
# Microsoft Excel install or xlwings/COM automation is needed (runs on Linux).
et_xmlfile==2.0.0            # Excel XML writer dependency
numpy==2.4.6                 # numeric arrays
openpyxl==3.1.5              # headless Excel workbook read/write (replaces xlwings)
pandas==3.0.3                # data manipulation

# Command-line workflow (python -m fna ...)
typer==0.15.1                 # CLI framework (fna.cli)
click==8.1.7                  # pinned: typer 0.15.1 + click>=8.2 breaks --help rendering
tabulate==0.9.0               # Markdown tables in make-report

# Representative-day refresh and full-year benchmark
entsoe-py==0.8.0             # ENTSO-E Transparency client
lxml==6.1.1                  # XML parsing for entsoe-py
scikit-learn==1.9.0          # K-means clustering of representative days
scipy==1.17.1                # clustering and numerical algorithms

# Monte Carlo PECD reading
pyarrow==24.0.0              # Parquet support for PECD files

# Plotting, visualization, and charting
beautifulsoup4==4.15.0        # HTML parsing
certifi==2026.5.20
charset-normalizer==3.4.7
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
idna==3.18
joblib==1.5.3
kaleido==1.3.0
kiwisolver==1.5.0
matplotlib==3.10.9
narwhals==2.22.1
pillow==12.2.0
plotly==6.8.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.2
soupsieve==2.8.4

# Optional and environment utilities
choreographer==1.3.0
logistro==2.0.1
orjson==3.11.9
packaging==26.2
platformdirs==4.10.0
python-dotenv
psutil==7.2.2               # memory monitoring during model runs
requests==2.34.2
simplejson==4.1.1
six==1.17.0
threadpoolctl==3.6.0
typing_extensions==4.15.0
urllib3==2.7.0
